(a) This declaration is incorrect since we are defining a double pointer, pointing at the address of an integer.
(b) This is incorrect because we are creating a pointer which points at the value 0. We need to assign an address value.
