#include <iostream>
#include <vector>
using std::cout; using std::endl; using std::vector; using std::string;

// bool compare(const matrix &, const matrix &);

vector<int> change_val(int param, std::vector<int>::iterator vector_iterator);
int count(const string &, char);

int main() {
	
	return 0;
}
